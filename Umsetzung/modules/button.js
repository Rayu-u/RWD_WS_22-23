/**
 *
 *
 * @param {string} cssColor as the color string
 * @param {string | string[]} text The text content of the button being made. In an array if should span multiple lines?
 */
const createButton = (activeColor, inactiveColor, text) => {
  const element = document.createElement("div");
};
